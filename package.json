{
  "name": "next-image-placeholder-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "turbo": "^2.3.3"
  },
  "pnpm": {
    "overrides": {
      "sharp": "^0.34.5"
    }
  },
  "packageManager": "pnpm@10.20.0"
}
